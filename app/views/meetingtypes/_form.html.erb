<%= form_for(meetingtype,:url => @url) do |f| %>
  <% if meetingtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meetingtype.errors.count, "error") %> prohibited this meetingtype from being saved:</h2>

      <ul>
      <% meetingtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, placeholder: 'Name', class: 'form-control'  %>
  </div>

  <div class="actions">
      <%= f.submit class: "btn btn-primary  btn-lg" %>
      <%= link_to 'Cancel', meetingtypes_path, class: "btn btn-default  btn-lg" %>

  </div>
<% end %>
